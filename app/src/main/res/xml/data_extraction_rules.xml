<?xml version="1.0" encoding="utf-8"?>
<!-- Data extraction rules (API 31+). Backup is disabled app-wide; these rules
     belt-and-braces exclude the browsing profile from any cloud/device transfer
     in case allowBackup is ever re-enabled. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="vin_browser.xml" />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="vin_browser.xml" />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </device-transfer>
</data-extraction-rules>
